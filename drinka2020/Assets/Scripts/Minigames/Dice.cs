using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dice : Minigame
{
    string title = "Dice";
    // Start is called before the first frame update
    void Start()
    {
        gameName = "Dice";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
