using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoInternet : Minigame
{
    // Start is called before the first frame update
    void Awake()
    {
        gameName = "No Internet";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
