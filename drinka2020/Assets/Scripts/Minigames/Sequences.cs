using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sequences : Minigame
{
    // Start is called before the first frame update
    void Awake()
    {
        gameName = "Sequences";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
